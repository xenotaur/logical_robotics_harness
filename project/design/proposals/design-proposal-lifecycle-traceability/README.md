# Design Proposal Lifecycle and Implementation Traceability

Status: `adopted`
Implementation status: `not_started`

Reading order:

1. [`00_proposal.md`](00_proposal.md) — adopts first-class design
   proposal decision lifecycle metadata, separate implementation
   lifecycle metadata, traceability fields, validation direction, and
   future tooling stages.

This proposal is design-only. It records the adopted direction for LRH
and does not implement parser, validator, organizer, CLI, or snapshot
behavior. Future implementation remains staged across parser and
validation model work, `lrh design organize`, snapshot reporting, and
dogfood migration.
