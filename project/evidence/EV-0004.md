---
id: EV-0004
title: Release tag CI validation for v0.2.3
type: test_result
status: recorded
related_work_items:
  - WI-RELEASE-TAG-CI
related_focus: []
source:
  kind: github_actions
  note: release tag validation workflow run for v0.2.3
summary_result: pass
artifacts:
  - release-artifacts-v0.2.3
metrics: {}
blocked_actions: []
modified_actions: []
approval_records: []
---

# Release tag CI validation for v0.2.3

The `WI-RELEASE-TAG-CI` work item is closed based on successful external GitHub Actions validation of the `v0.2.3` release tag.

## Evidence

- GitHub Actions run: https://github.com/xenotaur/logical_robotics_harness/actions/runs/25342434294
- Workflow: Release tag validation
- Trigger: tag push for `v0.2.3`
- Commit: `dd78e89`
- Result: success
- Artifact: `release-artifacts-v0.2.3`

## Acceptance-criterion checks recorded

The successful workflow result records that the release tag CI path completed these checks for `v0.2.3`:

- Triggered from the release-like pushed tag `v0.2.3`, matching the workflow's `v*.*.*` tag trigger.
- Checked out the tagged revision using `ref: ${{ github.ref }}`.
- Validated the tag format against the `vMAJOR.MINOR.PATCH` release-tag pattern.
- Installed development dependencies with the repository constraint file before release checks.
- Ran `scripts/version verify "$TAG_UNDER_TEST"` with `TAG_UNDER_TEST` set from `${{ github.ref_name }}`.
- Ran `scripts/release-smoke "$TAG_UNDER_TEST"` with the same tag under test; that smoke path performs a clean release rebuild, builds artifacts, installs the built wheel in a temporary virtual environment, and checks installed CLI behavior.
- Uploaded `dist/` and `.pytest_cache/` through the `release-artifacts-v0.2.3` artifact capture step for auditability.

## Interpretation

This confirms the release tag validation workflow ran successfully for the pushed `v0.2.3` tag and produced release artifacts suitable for auditability. It records completion evidence only; it does not introduce publishing automation or any CI workflow changes.

## Future maintenance note

Any GitHub Actions Node.js 20 deprecation warning observed on the run is non-blocking for this closeout and should be handled separately as future actions-version maintenance.
