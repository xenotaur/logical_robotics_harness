---
execution_id: 2026_07_23_02_53_52_SESSION_ID_DOCS
prompt_id: PROMPT(AD_HOC:SESSION_ID_DOCS)[2026-07-23T02:43:23-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/409
commit: ada1ec2
created_at: 2026-07-23T02:53:52-04:00
agent: claude_app
instruction_source: ad_hoc conversation — session-ID documentation fixes (part 1 of 2; companion backfill PR tracked separately)
session_transcript: claude-app:4c3d03d6-6ebd-418a-86b2-6f4041feb9db
---

# Summary

Record the desktop-app session-identifier model (host vs. child SDK session
ids) and the standing transcripts-never-committed decision:

- Added a 2026-07-23 decision-log entry to `project/memory/decision_log.md`
  stating that session transcripts are never committed to this repository;
  only the pointer `session_transcript: claude-app:<uuid>` is stored.
- Rewrote the `### session_transcript` section of
  `execution-session-reference.md` (both `src/lrh/skills/lrh-implement/` and
  `.claude/skills/lrh-implement/` mirrors), which previously defined the
  session id as the JSONL filename stem. It now documents the host id
  (`local_<uuid>`, the durable app-level key) vs. child SDK id (JSONL stem)
  distinction, the canonical stored form `claude-app:<host-uuid-stem>`
  (`local_` prefix stripped), and the in-session env-var sources
  (`CLAUDE_CODE_HOST_SESSION_ID`, `CLAUDE_CODE_SESSION_ID`).

# Result

Both documentation changes landed on branch `xenotaur/chore/session-id-docs`
as PR #409. Skill mirrors verified in sync (`diff -r` shows only the
pre-existing structural src-only entries). The `session_transcript` field of
this record is populated in-session from `CLAUDE_CODE_HOST_SESSION_ID` per
the newly documented convention.

Prior-art check: the closeout/session-URL design work in progress is
adjacent but explicitly out of scope for this task; no existing artifact
records the transcripts-never-committed decision (it predates the LRH
control plane).

# Validation

- `scripts/version tools` — lrh 0.2.5.dev83+g40da6c798, Python 3.11,
  ruff 0.15.12, black 26.3.1 (LRH env)
- `scripts/format --check --diff` — clean (179 files unchanged)
- `scripts/lint` — clean
- `scripts/test` — 796 tests, OK
- `lrh validate` — 0 errors, 0 warnings
- `diff -r src/lrh/skills .claude/skills` — no new drift

# Follow-up

- Companion PR 2: backfill/normalize `session_transcript` values in existing
  execution records (5 frontmatter `pending` records; 24 records with the
  legacy `claude-app:local_<uuid>` prefixed form), tracked under
  `PROMPT(AD_HOC:SESSION_TRANSCRIPT_BACKFILL)[2026-07-23T02:43:23-04:00]`.
