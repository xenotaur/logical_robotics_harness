---
execution_id: 2026_08_14_01_20_27_BOLT_PERF_WALK_5525376771513186514_SELFREVIEW
prompt_id: PROMPT(AD_HOC:BOLT_PERF_WALK_5525376771513186514_SELFREVIEW)[2026-08-14T01:20:21+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/403
commit: 9c6eee84af8e7adc5d1665e7d001b3950d8df9bd
agent: claude_app
instruction_source: https://github.com/xenotaur/logical_robotics_harness/pull/403
session_transcript: claude-app:0d8e0e17-f67a-46e9-923f-c4ca410aa7e8
created_at: 2026-08-14T01:20:27+00:00
---

# Summary

`/lrh-self-review` PR-mode pass on PR #403, used as the `/lrh-confirm-fixes`
Step 8 substitute review signal — no automatic Copilot/Codex response had
landed on the `_CONFIRM` commit (`9c6eee84`) after ~18 hours. `rerun_of`
left empty — no primary implementation record exists (bot-authored PR,
never went through `/lrh-implement`).

# Result

Dispatched a cold-context subagent with the PR URL, HEAD SHA, and full
orientation on the prior round's 3 fixes, explicitly told to re-verify
each rather than trust the resolved-thread state. It confirmed all 3
prior fixes hold up (dead-code removal, `_IGNORED_DIRS` hoist +
`topdown=True`, and the Windows case-insensitivity fix itself), but found
**one genuine new bug introduced by the case-insensitivity fix**:
`survey_python_tree`'s `readme_files` detection (`_walk_files(root,
"README.md")`) became case-insensitive, but the sibling
`documentation_files` exclusion filter (`if path.name != "README.md"`)
stayed case-sensitive — so a lowercase `readme.md` would be picked up by
both walks and double-listed.

**Independently re-verified** (mandatory, not delegated): read
`src/lrh/assist/sourcetree_surveyor.py:261-267` directly, confirmed the
case mismatch, then reproduced the duplication with a monkeypatched
`_walk_files` call — `readme_files: ['README.md', 'readme.md']`,
`documentation_files: ['other.md', 'readme.md']` (duplicate). The
subagent's finding held up exactly as described.

**Fixed**: `documentation_files`'s filter changed to
`path.name.lower() != "readme.md"` (`sourcetree_surveyor.py:267`). Added
a regression test (`test_lowercase_readme_is_not_duplicated_into_documentation_files`,
`tests/assist_tests/sourcetree_surveyor_test.py`) — this project's own
convention that new/changed Python logic needs test coverage, and the
subagent had confirmed zero existing coverage of `_walk_files`/README
detection at all.

**Separately discovered while validating the fix — a real testing-infra
bug, not a PR-403 issue:** running `scripts/test`/`python -m unittest
discover -s tests` from this worktree resolves `import lrh` to a
*different* checkout entirely (multiple stale `__editable__.lrh-*.pth`
files in the conda env's site-packages put several other repo/worktree
paths ahead of this one on `sys.path`; this worktree's own `src/` isn't
on `sys.path` at all without an explicit override) — this is the known
`project_worktree_pythonpath_gotcha` memory. It means **every prior
`scripts/test` run in this PR's review-response and confirm-fixes rounds
validated the wrong checkout's code**, not this diff. Re-ran with
`PYTHONPATH="$(pwd)/src"` explicitly set and confirmed `import
lrh.assist.sourcetree_surveyor` now resolves to this worktree before
trusting any test result for this round.

# Validation

- `PYTHONPATH="$(pwd)/src" python -c "import lrh.assist.sourcetree_surveyor as m; print(m.__file__)"` — confirmed resolves to this worktree, not a stale checkout
- `PYTHONPATH="$(pwd)/src" scripts/test` — 1087 tests, OK (includes the new regression test)
- `scripts/format --check --diff`, `scripts/lint` — clean (re-ran `pip install --upgrade "black==26.3.1" "ruff==0.15.12"` again mid-run; tool versions had drifted again)
- `lrh validate` — 0 errors, 1 pre-existing unrelated warning

# Follow-up

None beyond the fix above, routed through `/lrh-confirm-fixes` Step 8's
non-thread-finding path in this same run.
