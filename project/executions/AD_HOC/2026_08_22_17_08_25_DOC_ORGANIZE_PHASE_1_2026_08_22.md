---
execution_id: 2026_08_22_17_08_25_DOC_ORGANIZE_PHASE_1_2026_08_22
prompt_id: PROMPT(AD_HOC:DOC_ORGANIZE_PHASE_1_2026_08_22)[2026-08-22T16:55:01+00:00]
work_item: AD_HOC
status: landed
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/605
commit: 6eaf0f1c779695cd4797a214d7cc576fa52e27bd
created_at: 2026-08-22T17:08:25+00:00
agent: claude_app
instruction_source: project/audits/docs/docs-audit-2026-08-21.md
session_transcript: claude-app:3bfe1290-c27a-4ffe-82f5-e718adbd8319
---

# Summary

Implements `/lrh-doc-organize` phase 1 against
`project/audits/docs/docs-audit-2026-08-21.md`'s "Proposed first PR
scope": adds `docs/reference/cli/memory.md` documenting all 10
`lrh memory` subcommands, plus one index line each in
`docs/reference/cli/README.md` and `docs/README.md`.

# Result

Created `docs/reference/cli/memory.md`, modeled on the `lrh secrets`
reference-page precedent. Content verified directly against `--help`
output and source (`src/lrh/prompt_workflow_memory.py`,
`src/lrh/memory_workflow.py`) on a correctly-resolved current `main`
checkout (`PYTHONPATH=src`, not the stray editable install pointing at
a different checkout — a known, previously-memorized gotcha) rather
than assumed from the design proposal.

While verifying `transfer`/`import`'s overwrite behavior, discovered
that `WI-LRH-MEMORY-TRANSFER-SAFETY` (the WI this reference page links
to as a "known gap") is marked `status: resolved` on `main` with a
resolution claiming the fix was "implemented and merged in PR #597" —
but `git log -- src/lrh/prompt_workflow_memory.py` shows the file's
last touching commit is PR #594 (`4acd447e`), and PR #597's own diff
(`git show 8f40e33a --stat`) contains only the WI-creation planning
artifact and execution records, zero `src/` changes. Directly
re-verified the actual guard in `_write_memory_into_dir` (line ~294):
still only checks `existing_authored_by != agent` — no same-agent
guard, no legacy-no-authored_by guard, no snapshot. This documentation
was written to reflect the real, currently-shipped behavior (same-agent
overwrite unconditional and unsnapshotted) rather than the WI's false
resolution claim, and the PR description flags the discrepancy
separately. Not fixed in this PR — out of scope for a doc-organize run,
and the other session actively working that WI's worktree
(`memory-transfer-safety-d0ca89`) was not otherwise touched.

Two links added to the new page (`WI-LRH-MEMORY-TRANSFER-SAFETY`, the
docs audit) resolve correctly to their current paths — re-checked after
a mid-implementation `origin/main` rebase moved the WI file from
`proposed/` to `resolved/`.

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- `git diff --name-only origin/main..HEAD -- '*.py'` — empty; diff is
  docs-only.
- Both new `project/`-relative links confirmed to resolve
  (`test -f <target>`).
- `scripts/format --check --diff` / `scripts/lint` — failed on a
  pre-existing local tool-version mismatch (installed `ruff 0.15.0`/
  `black 25.11.0` vs. pinned `0.15.12`/`26.3.1`), unrelated to this
  docs-only change.
- `scripts/test` — 9 failures / 25 errors out of 1279 tests, from a
  stale packaged smoke-test wheel (`dist/lrh-0.2.1...`, missing
  `read`/`search` CLI wiring present in the actual source) and
  unrelated `sessions_test.py` nested-mirror assertions — pre-existing
  environment drift, not caused by this docs-only diff.

# Follow-up

- Phase 2 (how-to guides for `sync` backup/restore and
  `export`/`import`/`transfer` cross-project moves) remains, per the
  audit's phased plan — run `/lrh-doc-organize --phase 2` after this
  phase merges.
- `lrh sessions`'s identical docs gap is out of scope, per the audit.
- The `WI-LRH-MEMORY-TRANSFER-SAFETY` false-resolution discrepancy
  found above needs separate attention — surfaced to the user in this
  session, not auto-corrected here.
