---
execution_id: 2026_08_22_19_34_22_DOC_ORGANIZE_PHASE_1_2026_08_22_REVIEW
prompt_id: PROMPT(AD_HOC:DOC_ORGANIZE_PHASE_1_2026_08_22_REVIEW)[2026-08-22T19:34:15+00:00]
work_item: AD_HOC
status: landed
rerun_of: 2026_08_22_17_08_25_DOC_ORGANIZE_PHASE_1_2026_08_22
pr: https://github.com/xenotaur/logical_robotics_harness/pull/605
commit: 6eaf0f1c779695cd4797a214d7cc576fa52e27bd
created_at: 2026-08-22T19:34:22+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/logical_robotics_harness/pull/605
session_transcript: claude-app:3bfe1290-c27a-4ffe-82f5-e718adbd8319
---

# Summary

Addressed 6 open review comments on PR #605 (`lrh memory` CLI
reference page) — 4 distinct findings, 2 of them independently flagged
by both `copilot-pull-request-reviewer` and `chatgpt-codex-connector`.

# Result

1. **Search-reachability claim wrong (Codex P2).** Docs said unindexed
   memories are "unreachable by `search`/`list`" — verified against
   `search_memories()` source: it scans `memory_dir.glob("*.md")`
   directly, not the index, so an unindexed file *is* still found by
   `search`. Fixed to say unreachable by `list` only.
2. **`sync` comparison mechanism overstated (Copilot).** Docs said
   "content-hash comparison" unconditionally. Verified: the real-sync
   path (`mirror_file_with_snapshot`) does use SHA-256, but `sync`'s
   own `--dry-run` path (`prompt_workflow_memory.py`) compares raw
   `read_bytes()` equality, not a hash. Reworded to "byte-for-byte
   comparison," accurate for both paths.
3. **First `transfer` example unrunnable (Codex P2).** The example had
   neither `--name` nor `--agent`; verified `transfer_memories()`
   routes through `_export_records_from_dir()` →
   `_require_export_filter()`, which rejects an unfiltered call. Added
   `--agent claude_app` to the example and documented the inherited
   filter requirement explicitly.
4. **CLI index one-liner too narrow (Copilot).** Named only
   validate/sync/search/transfer; expanded to name all 10 subcommands.
5. **Broken-link findings (Copilot + Codex P2), now moot.** Both
   flagged the `WI-LRH-MEMORY-TRANSFER-SAFETY` link as pointing at a
   nonexistent `proposed/` path — accurate at review time, because the
   WI had been (erroneously) marked `resolved` by a separate session's
   closeout. The user corrected that closeout independently; the WI is
   back at `proposed/` on `main`, so the link (never actually edited,
   since it was correct in the original commit and is correct again
   now) resolves. No doc change needed for this pair.

Mid-fix, `origin/main` advanced twice (the WI-closeout correction, plus
an unrelated PR #604 merge). Rebased once before starting the fixes,
then hit a rejected push after the fixes were committed on top of a
now-stale rebase; force-push was denied by policy, so resolved via a
non-destructive `git merge origin/<branch>` instead (2 files, 5
hunks — all resolved in favor of the fixed content; verified no
conflict markers remained, both `project/`-relative link targets exist
on disk, and `lrh validate` passed post-merge).

Publication outcome: **Pushed directly** — commit `0e18f3fd` (a merge
commit; no history rewritten) on the existing open PR branch
`xenotaur/chore/doc-organize-phase-1-2026-08-22`.

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- `git diff --name-only origin/main..HEAD -- '*.py'` — empty, diff
  remains docs-only.
- Both `project/`-relative link targets re-confirmed to exist
  post-merge.
- `scripts/format`/`scripts/lint` — same pre-existing local
  tool-version mismatch as the primary record, unrelated to this
  docs-only change.

# Follow-up

- None from this round.
