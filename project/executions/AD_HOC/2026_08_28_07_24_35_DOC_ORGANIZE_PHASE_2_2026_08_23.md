---
execution_id: 2026_08_28_07_24_35_DOC_ORGANIZE_PHASE_2_2026_08_23
prompt_id: PROMPT(AD_HOC:DOC_ORGANIZE_PHASE_2_2026_08_23)[2026-08-28T06:48:16+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/644
commit: 
created_at: 2026-08-28T07:24:35+00:00
agent: claude_app
instruction_source: project/audits/docs/docs-audit-2026-08-21.md
session_transcript: pending
---

# Summary

Implements `/lrh-doc-organize` phase 2 against
`project/audits/docs/docs-audit-2026-08-21.md`'s "Recommended phased
PRs" § PR 2: two how-to guides for `lrh memory` (`sync` backup/restore,
and `export`/`import`/`transfer` cross-project moves), plus index
entries and a forward-link fix in phase 1's reference page.

# Result

Created `docs/how-to/back-up-and-restore-project-memory.md` and
`docs/how-to/move-memories-between-projects.md`, modeled on the
`lrh secrets` how-to precedent. Every command and output shown in both
guides was verified against live runs in a scratch environment
(`PYTHONPATH=src`, correctly resolved current-checkout source per this
project's known editable-install gotcha) — not written from the audit's
prose or from memory. Discovered mid-verification that
`WI-LRH-MEMORY-TRANSFER-SAFETY` genuinely landed since phase 1 (PR
#606, commit `9ebce502` — confirmed via `git log` to actually touch
`src/lrh/prompt_workflow_memory.py`, unlike an earlier false-resolution
attempt on the same WI that this session had separately caught and
flagged during phase 1). The new `move-memories-between-projects.md`
guide documents the current, fixed behavior directly (real
`--force`-required-for-any-overwrite semantics, real snapshot-to-`history/`
filenames, real bare-slug-vs-path resolution error text) rather than
the stale behavior phase 1's reference page still describes.

Also fixed `docs/reference/cli/memory.md`'s "Related how-to guidance"
section, which said "No how-to guide exists yet" — made stale by this
very phase's own deliverable — to link to both new guides, completing
the two-way link the audit's phased plan calls for ("each linking back
to PR 1's reference page"). Did not touch that same file's "Known gap"
section (which still describes the *old*, unfixed `transfer`/`import`
overwrite behavior) or its `WI-LRH-MEMORY-TRANSFER-SAFETY` link (now
pointing at a `resolved/` WI from a `proposed/`-path link) — that is a
larger, WI-driven update reserved for `/lrh-doc-work`, out of this
phase's one-phase-per-PR scope. Flagged explicitly to the user, who
asked that it not be lost — see Follow-up.

# Validation

- `lrh validate` — 0 errors; 80 pre-existing warnings from an unrelated
  concurrent PR's new lint rule (`FRONTMATTER_LINT_UNSAFE_SCALAR`),
  none touching this diff's files.
- `scripts/format --check --diff` / `scripts/lint` — clean (unlike
  phase 1, no local tool-version mismatch this time).
- `git diff --name-only origin/main..HEAD -- '*.py'` — empty, diff
  remains docs-only.
- All new/edited links independently confirmed to resolve
  (`test -f <target>`).

# Follow-up

- Fix `docs/reference/cli/memory.md`'s stale "Known gap" section and
  its `WI-LRH-MEMORY-TRANSFER-SAFETY` link path via
  `/lrh-doc-work WI-LRH-MEMORY-TRANSFER-SAFETY` — tracked separately
  (backlog entry and/or memory, per the user's explicit request) so it
  isn't lost.
- Phase 3 (the optional `docs/explanations/` page) remains deferred per
  the audit's own phased plan — not committed to.
- `lrh sessions`'s identical docs gap remains out of scope.
