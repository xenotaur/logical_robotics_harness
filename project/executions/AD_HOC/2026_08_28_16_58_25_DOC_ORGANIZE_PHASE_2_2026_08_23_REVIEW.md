---
execution_id: 2026_08_28_16_58_25_DOC_ORGANIZE_PHASE_2_2026_08_23_REVIEW
prompt_id: PROMPT(AD_HOC:DOC_ORGANIZE_PHASE_2_2026_08_23_REVIEW)[2026-08-28T16:57:58+00:00]
work_item: AD_HOC
status: landed
rerun_of: 2026_08_28_07_24_35_DOC_ORGANIZE_PHASE_2_2026_08_23
pr: https://github.com/xenotaur/logical_robotics_harness/pull/644
commit: 03ed9acc43ecda40bac175570fc9681028c3c7f4
created_at: 2026-08-28T16:58:25+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/logical_robotics_harness/pull/644
session_transcript: claude-app:3bfe1290-c27a-4ffe-82f5-e718adbd8319
---

# Summary

Addressed 5 open review comments on PR #644 (`lrh memory` how-to
guides) — 4 fixed, 1 skipped with rationale as out-of-scope, already
tracked separately.

# Result

1. **Sync snapshot-dedup claim wrong (Copilot + Codex P2, same
   finding).** Docs said the snapshot filename is "keyed by the prior
   content's hash, so the same version is never snapshotted twice."
   Independently re-verified live with an A→B→A→B cycle test in a
   scratch project: content-X's short hash (`d8528a8ca686`) appeared
   in two snapshot filenames under two different timestamps
   (`165556Z` and `165557Z`) — confirming the filename includes a
   timestamp and dedup is not hash-only for `sync`. Fixed the claim to
   describe the actual behavior; also called the hash a "12-character
   short hash," not a full digest (Copilot's second point).
2. **Import/transfer "no-op" description misleading (Copilot).**
   Docs said a byte-identical write is "a no-op ... nothing changes."
   Re-verified: the write still proceeds and reports `wrote:`; only
   `--force` and the snapshot are skipped. Reworded.
3. **`cp` restore example broken for full-corpus-loss recovery (Codex
   P2).** `cp` does not create missing parent directories, so the
   documented recovery command fails exactly in the scenario the guide
   advertises. Added a recursive `cp -r` for whole-corpus restore, and
   an explicit `mkdir -p` for the single-file case.
4. **`docs/reference/cli/memory.md` contradicts the new how-to guide
   (Codex P2) — skipped, tracked separately.** Valid finding: the
   reference page's "Known gap" sections and `WI-LRH-MEMORY-TRANSFER-SAFETY`
   links are stale (confirmed already, before this PR, during the
   doc-organize confirm gate). Not fixed here: the user explicitly
   confirmed at this PR's own confirm gate that this fix is
   `/lrh-doc-work`'s scope, not doc-organize's, and asked it be tracked
   separately rather than lost — done via a `project/design/backlog.md`
   entry and an agent memory (`project_memory_md_stale_vs_transfer_safety_fix`),
   both added directly to `main` before this PR opened. This is a
   Problematic-comment-adjacent case: the finding is factually correct
   but conflicts with an already-made, already-tracked scope decision,
   not an oversight to fix inline.

Publication outcome: **Pushed directly** — commit `34850b9c` on the
existing open PR branch `xenotaur/chore/doc-organize-phase-2-2026-08-23`.

# Validation

- `lrh validate` — 0 errors, 0 warnings.
- `scripts/format --check --diff` / `scripts/lint` — clean.
- Each of the 3 fixed claims independently re-verified against live
  command runs in a scratch environment before and after the fix, not
  merely accepted from the review comments.

# Follow-up

- None from this round beyond the already-tracked backlog item.
