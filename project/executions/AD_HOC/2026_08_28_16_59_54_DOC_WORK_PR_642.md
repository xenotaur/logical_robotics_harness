---
execution_id: 2026_08_28_16_59_54_DOC_WORK_PR_642
prompt_id: PROMPT(AD_HOC:DOC_WORK_PR_642)[2026-08-28T16:55:39+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/647
commit: 
created_at: 2026-08-28T16:59:54+00:00
agent: claude_app
instruction_source: 'command lrh-doc-work https://github.com/xenotaur/logical_robotics_harness/pull/642, auto-detected from the current branch (user asked for this doc gap to be closed after /lrh-work-remains flagged it)'
session_transcript: pending
---

# Summary

Ran `/lrh-doc-work` against PR #642 (`WI-FRONTMATTER-MIGRATION-LINT-GUARD`),
auto-detected from the current branch. Documented the PR's two new
user-visible surfaces: `lrh project doctor --fix-frontmatter`/`--apply`
and `lrh validate`'s new `FRONTMATTER_LINT_UNSAFE_SCALAR` warning
category.

# Result

- `docs/reference/cli/doctor.md` (new): full reference for `lrh project
  doctor` -- it had no prior documentation at all. Covers the pre-existing
  `--json`/`--strict` flags and the new `--fix-frontmatter`/`--apply`
  migration flags, including the `--project-root` vs. `lrh validate`'s
  `--project-dir` convention mismatch and the nested-mapping safety
  guarantee.
- `docs/reference/cli/validate.md`: added a note documenting the new
  `FRONTMATTER_LINT_UNSAFE_SCALAR` report-only warning category, with a
  cross-link to `doctor.md`.
- `docs/reference/cli/README.md`: added the `doctor` entry to the command
  index.
- Flagged but not fixed: `lrh project init` (a sibling `project`
  subcommand) is also undocumented, noticed while checking `doctor`'s
  neighborhood -- out of this PR's scope, noted in the PR body as a
  candidate for a future `/lrh-doc-audit`/`/lrh-doc-organize` pass.

# Validation

- `scripts/format --check --diff`: clean.
- `scripts/lint`: clean.
- `scripts/test`: 1471 tests, all pass.
- `lrh validate`: 0 errors, 0 warnings.
- New/updated doc links (`doctor.md`, `validate.md` cross-link,
  `validate-a-project.md`) manually confirmed to resolve to existing
  files.

# Follow-up

- `lrh project init` remains undocumented -- see Result above.
