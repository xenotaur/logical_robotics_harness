---
execution_id: 2026_09_26_05_19_23_WI_LOCAL_AGENT_001
prompt_id: PROMPT(WI-LOCAL-AGENT-001:WI_LOCAL_AGENT_001)[2026-09-26T02:47:36+00:00]
work_item: WI-LOCAL-AGENT-001
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/735
commit: 
created_at: 2026-09-26T05:19:23+00:00
agent: claude_app
instruction_source: project/work_items/active/WI-LOCAL-AGENT-001.md
session_transcript: pending
---

# Summary

PR B of the approved `WI-LOCAL-AGENT-001` plan, run through `/lrh-execute`. It
adds the experiment pre-registration and the stage-0 briefing prototype. This is
**partial progress**: the live pilot, sanitized results, and the owner's
stop/revise/proceed decision come in a later PR (PR C). The work item stays
`active`, and closeout of this PR must not resolve it. `WI-LOCAL-AGENT-002` is
unaffected.

# Result

Work item Required Changes addressed in this PR:

1. **Pre-run choices recorded** in `experiments/01_local_agent_briefing/README.md`:
   - hardware and the model manifest and layer digests;
   - budgets and local-only checks;
   - store path and retention;
   - the 12-task corpus;
   - the binding floor and advisory targets.

   They are committed ahead of the prototype code and before any live run.
2. **Context.**
   - Tracked content at a pinned commit, read via `git archive` or Git
     objects.
   - Readiness diagnostics come from `evaluate_readiness`,
     `render_run_packet_from_work_item`, and `render_ready_work_item_request`,
     and are preserved verbatim.
   - Each source records commit, blob, sha256, and line range.
   - Private, binary, untracked, and over-budget content is rejected or
     visibly truncated.
   - Each packet is approved by sha256 and re-verified before use.
3. **Model, context, and recorder seams, plus the runner.**
   - Typed seams, and a single-call runner with pinned prompt, policy,
     backend, and model versions.
   - Input, output, token, and wall-time bounds.
   - Seven explicit outcomes, with failures recorded.
4. **Private store.**
   - Single writer, with an atomic manifest and a JSONL event log.
   - A truncated tail is recovered as `incomplete`.
   - Inspection and sanitized export are available.
   - Raw content stays outside Git.
5. **Experiment convention.** `experiments/README.md` defines it, and
   `01_local_agent_briefing/` is created.
6. **Opt-in tests.** 58 fake-backend `unittest` cases, run via
   `experimental/local_agent/test` and outside default discovery.
7. **Pilot.** Not in this PR. It is pre-registered and pending.

Prior art: the check was present in the work item and refreshed at activation.
No duplication was found.

# Validation

- `scripts/version tools`: Python 3.11.16, ruff 0.15.12, black 26.3.1
  (env `LrhLocalAgent`).
- `scripts/format --check --diff` and `scripts/lint`, both default and on
  `experimental/local_agent`: clean.
- `scripts/test --log`: Ran 1795 tests, OK. The prototype is not discovered.
- `experimental/local_agent/test`: Ran 58 tests, OK. No network or live model.
- `lrh validate`: 0 errors, 0 warnings.
- Packet dry runs for all 12 corpus tasks on the real LRH and LCATS
  checkouts. All pins resolve exactly, and all packets fit the budgets.
- Diff-mode self-review: see
  `project/executions/AD_HOC/2026_09_26_05_18_25_WI_LOCAL_AGENT_001_SELFREVIEW.md`.
  Nine findings were fixed.

# Follow-up

- **PR C:** the owner starts Ollama, runs the smoke check, the tuning tasks
  (at most 3 prompt iterations), and the frozen held-out run. Then B0/B1
  scoring, sanitized results, and the decision. That PR's closeout resolves
  `WI-LOCAL-AGENT-001`.
