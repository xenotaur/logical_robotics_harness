---
execution_id: 2026_08_29_17_13_27_WI_PII_SCAN_CLI
prompt_id: PROMPT(WI-PII-SCAN-CLI:WI_PII_SCAN_CLI)[2026-08-29T16:54:02+00:00]
work_item: WI-PII-SCAN-CLI
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/654
commit: f8eab566
created_at: 2026-08-29T17:13:27+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-PII-SCAN-CLI.md
session_transcript: "claude-app:cf93c405-ed0f-409d-946f-7451a1cb2f7c"
---

# Summary

Implemented `WI-PII-SCAN-CLI`: the final integration item wiring the
already-implemented `src/lrh/pii` package into the `lrh` CLI as
`lrh pii scan`, per `PROP-LRH-PII-SCAN` Decision 1.

# Result

- `src/lrh/pii/scan.py` (new): `run_scan(project_root, out_dir,
  config_path=None) -> ScanResult`, `format_text`, `format_json` —
  mirrors `lrh.secrets.scan`'s own shape. Orchestrates
  `config.load_config` → `enumerate.enumerate_added_paths` →
  `layer1.flag_paths` → `layer2.content_findings_for_paths` →
  `output.build_findings` → `allowlist.load_allowlist` +
  `output.filter_allowlisted` → writes `pii_findings.json`.
- `src/lrh/pii/config.py`: `load_config` gained an optional
  `config_path` parameter overriding project-root auto-discovery, for
  the CLI's `--config` flag.
- `src/lrh/cli/main.py`: added a `pii` subcommand group
  (`lrh pii scan --project-root --out-dir --config --format`),
  following the existing `secrets` group's exact dispatch pattern.
- `tests/pii_tests/scan_test.py` (new, 6 tests), `tests/cli_tests/pii_test.py`
  (new, 7 tests), `tests/pii_tests/config_test.py` (2 new tests).

**Self-review finding (found and fixed before push):** diff-mode
`/lrh-self-review` found that the CLI dispatch only caught
`pii_config.PiiConfigError` — any other git-plumbing failure in the
pipeline (a non-git `--project-root`, or `layer2.Layer2ContentReadError`
from an unexpected `git show` failure) surfaced as a raw, unhandled
traceback instead of a clean CLI error. Independently reproduced before
accepting: `lrh pii scan --project-root <non-git-dir> --out-dir ...`
produced a `subprocess.CalledProcessError` traceback with no clean
message. Fixed by also catching `Layer2ContentReadError` and
`subprocess.CalledProcessError` in the dispatch, both reporting a clean
`error: ...` message and exit code 2 (matching `PiiConfigError`'s
convention). Re-verified the fix produces a clean error instead of a
traceback. Added two regression tests
(`test_lrh_pii_scan_reports_non_git_project_root_cleanly`,
`test_lrh_pii_scan_reports_layer2_content_read_error_cleanly`).

Also manually smoke-tested `lrh pii scan --project-root . --out-dir
<dir>` against this real repo (per the WI's own literal acceptance
criterion, not just the automated tests): ran successfully, wrote 3
findings for a known PDF test fixture already present in this repo's
history.

# Validation

- `tests.pii_tests.scan_test` + `tests.cli_tests.pii_test` +
  `tests.pii_tests.config_test` — 27/27 pass.
- Full suite: `python -m unittest discover -s tests -p '*_test.py'` —
  1534 tests, OK.
- `scripts/format --check --diff` / `scripts/lint` — clean.
- `lrh validate` — 0 errors, 2 pre-existing unrelated warnings
  (`WI-GATE-STALENESS-INSTALLED-TARGET-FINGERPRINT.md`, not touched by
  this change).
- Manual end-to-end run of `lrh pii scan` against this real repo,
  matching the WI's own acceptance criterion.
- Diff-mode `/lrh-self-review` (cold-context subagent): 1 finding,
  independently re-verified by direct reproduction, fixed.

# Follow-up

- None beyond the standard review-response/confirm-fixes/merge/closeout
  chain for PR #654. This is the last work item in `WS-PII-SCAN` —
  closeout will offer to close the workstream once this lands.
