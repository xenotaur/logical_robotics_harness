---
execution_id: 2026_08_21_06_37_20_WI_SECRETS_PURGE
prompt_id: PROMPT(WI-SECRETS-PURGE:WI_SECRETS_PURGE)[2026-08-21T06:05:44+00:00]
work_item: WI-SECRETS-PURGE
status: landed
rerun_of: 
pr: https://github.com/xenotaur/logical_robotics_harness/pull/584
commit: 9bb90d921b8ae1a065e43b95a130a0fbda5108e3
created_at: 2026-08-21T06:37:20+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-SECRETS-PURGE.md
session_transcript: claude-app:d6f0fa6c-efde-4a0e-9394-feecdf190d9e
---

# Summary

Implemented `WI-SECRETS-PURGE`: `lrh secrets purge`, wrapping
`git-filter-repo` to rewrite a disposable mirror clone scoped to
explicit refs, verify the rewrite removed every listed secret, and
print — but never execute — the resulting `git push --force` command.
Run via `/lrh-execute`'s inlined `/lrh-implement` (Step 3), following
the `/lrh-execute` chain authorization gate confirmed at the start of
this run. `depends_on: [WI-SECRETS-SCAN, WI-SECRETS-REVIEW]` was
verified resolved before this run started. This is the third and final
work item under `WS-SECRETS-COMMAND`.

# Result

- Created `src/lrh/secrets/purge.py`: `run_purge()` orchestrates the
  flow. `--refs-file` is mandatory (hard failure if missing/empty
  before any clone). `--replacements` must be `lrh secrets review
  --apply`'s output — enforced at runtime via `MARKER_LINE`
  (`# lrh-secrets-reviewed v1`, imported directly from
  `lrh.secrets.review` rather than duplicated), stripped before the
  remainder reaches `git-filter-repo`. Post-rewrite verification
  (`secret_still_present`) uses `git log --all -S<secret>` with no
  `--pickaxe-regex`, verifying secrets as literal strings. A failed
  verification is a hard `exit(1)` with no push command printed. On
  success, prints the push command per ref plus the manual-step
  reminders (notify collaborators/branch-owners; file a host-support
  request if the repo was ever public). `--dry-run`/`--apply` are
  mutually exclusive. No code path invokes `git push` anywhere in this
  module.
- Wired `secrets purge` into `src/lrh/cli/main.py` under the existing
  `secrets` subparser group from `WI-SECRETS-SCAN`/`WI-SECRETS-REVIEW`;
  updated the `secrets requires a subcommand` fallback message to
  mention all three subcommands.
- Created `tests/secrets_tests/purge_test.py` (fully mocked/hermetic,
  15 tests): refs-file/marker-line validation, dry-run performs no
  clone, `--apply` success/failed-verification paths, literal-string
  verification regression test with a metacharacter-bearing secret
  (`ab+c`), and a clean-error regression test for a missing `origin`
  remote.
- Created `tests/smoke/secrets_purge_smoke.py`: a real `git-filter-repo`
  end-to-end test (mirror-clone + rewrite + verify against a throwaway
  fixture repo), `skipUnless(shutil.which("git-filter-repo"), ...)`
  -gated per `AGENTS.md`'s testing policy, run via `scripts/smoke`.
- Extended `tests/cli_tests/secrets_test.py` with 9 new tests: `--help`
  coverage, requires-subcommand mentioning `purge`, missing-refs-file
  and missing-marker clean-error paths, `--dry-run`/`--apply` mutual
  exclusivity, dry-run success text, and dispatch-delegation.

**Pre-push diff-mode self-review** (dispatched as a cold-context
subagent per `/lrh-implement` Step 7.5): independently reviewed the
full staged diff, ran the test suites and the real smoke test directly,
and confirmed all safety invariants (refs-file mandatory, marker-line
gate, literal-string verification, no `git push` code path, failed
verification prints no push command, mutual exclusivity) hold. Found
two genuine bugs, both independently re-verified by direct reproduction
before and after the fix:
1. `default_source()` (resolving `--source` from the project's `origin`
   remote) ran before `--refs-file`/`--replacements` validation and
   raised a raw `CalledProcessError` traceback instead of a clean error
   when no `origin` remote existed — reproduced directly against a bare
   `git init` repo, even for a plain `--dry-run` with a missing refs
   file. Fixed by reordering `run_purge()` to validate inputs first and
   wrapping the remote-resolution failure as `PurgeInputError`.
2. `--dry-run` never called `check_filter_repo_available()`, contradicting
   `WI-SECRETS-PURGE.md`'s Required Changes item 1 ("--dry-run: validate
   all inputs ... binaries present"). Fixed by moving the availability
   check before the dry-run early return; updated the test that had
   asserted the opposite behavior.

# Validation

- `scripts/version tools` — Black 26.3.1, Ruff 0.15.12 (confirmed
  correct this run, no reinstall needed)
- `scripts/format --check --diff` — clean, 211 files unchanged
- `scripts/lint` — all checks passed
- `PYTHONPATH="$(pwd)/src" scripts/test` — 1195 tests, OK
- `PYTHONPATH="$(pwd)/src" scripts/smoke` — real `git-filter-repo`
  mirror-clone/rewrite/verify smoke test passes (`git-filter-repo`
  available at `/opt/homebrew/bin/git-filter-repo` in this environment);
  two pre-existing, unrelated `version_install_smoke.py` failures
  observed (venv creation issues consistent with this session's own
  shared-conda-environment-pollution memory) — not touched by this WI's
  diff, not introduced by it
- `lrh validate` — 0 errors, 0 warnings
- `lrh secrets purge --help` — documents `--project-root`, `--source`,
  `--refs-file`, `--replacements`, `--mirror-dir`, `--dry-run`, `--apply`

# Follow-up

- This is the last work item under `WS-SECRETS-COMMAND`
  (`WI-SECRETS-SCAN`, `WI-SECRETS-REVIEW` both already merged) — once
  this PR lands, closeout should offer `WS-SECRETS-COMMAND` closeout and
  adoption of `PROP-LRH-SECRETS-COMMAND`.
- `session_transcript` populated from `$CLAUDE_CODE_HOST_SESSION_ID`;
  same session throughout this `/lrh-execute` run.
