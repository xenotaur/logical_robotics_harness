#!/usr/bin/env bash
set -euo pipefail

# Build LRH via Python's build module.
# Requires dev dependencies; run scripts/develop (canonical setup entrypoint).
echo "Building the lrh package..."
set -x

python -m build "$@"

# Turn off logging and print a blank line
{ set +x; } 2>/dev/null
echo
