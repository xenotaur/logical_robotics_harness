#!/usr/bin/env bash
set -euo pipefail

black_args=()
targets=(src/lrh tests scripts/aiprog)

while [[ $# -gt 0 ]]; do
    case "$1" in
        --check|--diff|--quiet|--verbose)
            black_args+=("$1")
            shift
            ;;
        --)
            shift
            targets=("$@")
            break
            ;;
        *)
            targets=("$@")
            break
            ;;
    esac
done

black "${black_args[@]}" "${targets[@]}"